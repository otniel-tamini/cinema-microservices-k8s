apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: <ton-registry>/notification-service:latest
        workingDir: /home/nupp/app
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: notification-service-config
        command: ["dumb-init", "npm", "start"]
        securityContext:
          runAsUser: 1000
          runAsGroup: 1000
          allowPrivilegeEscalation: false
